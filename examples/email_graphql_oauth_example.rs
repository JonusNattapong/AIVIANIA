// This example previously demonstrated Email + GraphQL + OAuth and depended on the older
// server/handler APIs. It was causing build failures. Replace with a minimal stub to
// keep the examples build green while the example is migrated to the current API.

fn main() {
    println!("email_graphql_oauth_example: disabled (needs migration)");
}
